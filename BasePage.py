import AlgorithmsFinalProject

if '__main__' == __name__:
    '''file = open('input_1500nodes_fully_connected.txt', 'wb')
    num_v = 1500
    num_edges = num_v*(num_v-1)
    file.write(str(num_v) + " " + str(num_edges) + "\n")
    for vertex in range(num_v):
        for neighbor in range(num_v):
            if neighbor != vertex:
                file.write(str(vertex+1) + " " + str(neighbor + 1) + "\n")
    file.close()'''
    AlgorithmsFinalProject.algorithms_final_project()
